clone ```git clone https://github.com/Hyperion51/ModelBotter.git & cd ModelBotter```

install rust --> https://rust-lang.org/tools/install/

run ```cargo run build```

run ```npm -i```

run ```node index.js```

feel free to make a pull request

discord @mauricemadagascar
